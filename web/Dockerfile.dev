# Imagem base leve com Node.js
FROM node:22-alpine

# Diretório de trabalho
WORKDIR /app

# Copia apenas os ficheiros de dependências primeiro (cache eficiente)
COPY package.json package-lock.json ./

# Instala as dependências
RUN npm install

# Expõe a porta do Vite (padrão 5173)
EXPOSE 5173

# Comando padrão (podes sobrescrever no docker-compose)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
