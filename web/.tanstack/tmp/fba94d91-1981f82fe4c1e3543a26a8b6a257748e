import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/staff/tickets/$ticketId')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/staff/tickets/$ticketId"!</div>
}
